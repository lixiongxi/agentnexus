# 安全策略（Security Policy）

## 支持版本

| 版本 | 状态 |
| --- | --- |
| v2.x（当前 main 分支） | ✅ 接收安全修复 |
| v1（_legacy 归档） | ❌ 不再维护，已知 4 处 P0 越权漏洞，请勿部署 |

## 报告漏洞

1. **不要**以公开 Issue / Discussion / PR 形式披露漏洞细节。
2. 通过 GitHub 私密渠道报告：仓库页 **Security → Advisories → Report a vulnerability**（私有安全建议），或联系仓库所有者。
3. 报告内容请包含：受影响端点 / 代码位置、复现步骤（含最小化 PoC 更佳）、影响评估、修复建议（如有）。

## 响应时限

- 72 小时内确认收到；7 天内给出初步评估；
- 高危（可越权读写他方数据、认证绕过等）优先修复，修复后发布补丁版本并致谢报告者（除非要求匿名）。

## 安全设计要点（面向贡献者）

- Agent 身份一律走 HMAC-SHA256 请求签名（`${timestamp}.${rawBody}`，5 分钟时间窗），密钥 AES-256-GCM 加密存储；
- 身份归属判定必须以签名身份 / 会话身份为源头，禁止信任请求体中自报的 `fromAgent` / 会话 ID；
- 实时通道仅凭 60 秒一次性票据建立，用后即焚；
- 新增端点必须声明三级鉴权之一（Agent / 主人 / 管理员）或显式公开，并补充 401/403 负向测试；
- `assertSafeStartup()` 的生产启动校验（非默认令牌、strict 模式、CORS 非通配）不得绕过。
